<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Driver Dashboard - Ride Sharing System</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>

  <%- include('partials/navbar') %>

  <div class="page-wrapper">

    <div class="page-header">
      <h1>Driver Dashboard</h1>
      <p>Manage your rides and vehicles, <%= user.Name %></p>
    </div>

    <!-- Verification banners -->
    <% if (!identityVerification || identityVerification.Status === 'rejected') { %>
      <div class="alert alert-warning">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
        <span>
          <strong>Identity not verified.</strong>
          <a href="/auth/verify-identity" style="color:inherit;font-weight:600;text-decoration:underline;margin-left:6px">Submit your documents →</a>
        </span>
      </div>
    <% } else if (identityVerification.Status === 'pending') { %>
      <div class="alert alert-info">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
        <span>Identity verification is <strong>pending review</strong>. We'll notify you once it's processed.</span>
      </div>
    <% } %>

    <% const unregCount = vehicles.filter(v => !vehicleRegs.some(r => r.VehicleID === v.VehicleID)).length; %>
    <% if (vehicles.length > 0 && unregCount > 0) { %>
      <div class="alert alert-warning">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
        <span>
          <strong><%= unregCount %> vehicle<%= unregCount > 1 ? 's' : '' %> without official registration.</strong>
          <a href="/driver/register-vehicle" style="color:inherit;font-weight:600;text-decoration:underline;margin-left:6px">Register now →</a>
        </span>
      </div>
    <% } %>

    <!-- Stats -->
    <div class="stats-grid">
      <div class="stat-card">
        <div class="stat-label">Total Rides</div>
        <div class="stat-value"><%= rides.length %></div>
      </div>
      <div class="stat-card">
        <div class="stat-label">Active Rides</div>
        <div class="stat-value green"><%= rides.filter(r => r.Status === 'active').length %></div>
      </div>
      <div class="stat-card">
        <div class="stat-label">Vehicles Registered</div>
        <div class="stat-value"><%= vehicles.length %></div>
      </div>
      <div class="stat-card">
        <div class="stat-label">Available Seats (Total)</div>
        <div class="stat-value yellow"><%= rides.filter(r=>r.Status==='active').reduce((s,r)=>s+r.AvailableSeats,0) %></div>
      </div>
    </div>

    <div class="dashboard-grid">

      <!-- Add Ride -->
      <div class="card">
        <div class="card-header">
          <h2>Post a New Ride</h2>
          <div class="card-header-icon">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
          </div>
        </div>
        <div class="card-body">
          <form action="/driver/rides" method="POST">
            <div style="display:flex;flex-direction:column;gap:14px;">
              <div class="form-group">
                <label>Vehicle</label>
                <select name="vehicleId" required>
                  <option value="">Select your vehicle</option>
                  <% vehicles.forEach(v => { %>
                    <option value="<%= v.VehicleID %>"><%= v.Make %> <%= v.Model %> — <%= v.PlateNumber %></option>
                  <% }) %>
                </select>
              </div>
              <div class="form-grid">
                <div class="form-group">
                  <label>From</label>
                  <select name="originId" required>
                    <option value="">Origin</option>
                    <% locations.forEach(l => { %>
                      <option value="<%= l.LocationID %>"><%= l.LocationName %>, <%= l.City %></option>
                    <% }) %>
                  </select>
                </div>
                <div class="form-group">
                  <label>To</label>
                  <select name="destinationId" required>
                    <option value="">Destination</option>
                    <% locations.forEach(l => { %>
                      <option value="<%= l.LocationID %>"><%= l.LocationName %>, <%= l.City %></option>
                    <% }) %>
                  </select>
                </div>
                <div class="form-group">
                  <label>Departure</label>
                  <input type="datetime-local" name="departureTime" required>
                </div>
                <div class="form-group">
                  <label>Total Seats</label>
                  <input type="number" name="totalSeats" min="1" max="20" placeholder="e.g. 3" required>
                </div>
              </div>
              <button type="submit" class="btn btn-primary">Post Ride</button>
            </div>
          </form>
        </div>
      </div>

      <!-- Add Vehicle -->
      <div class="card">
        <div class="card-header">
          <h2>Register a Vehicle</h2>
          <div class="card-header-icon">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="1" y="3" width="15" height="13" rx="2"/><path d="M16 8h4l3 5v3h-7V8z"/><circle cx="5.5" cy="18.5" r="2.5"/><circle cx="18.5" cy="18.5" r="2.5"/></svg>
          </div>
        </div>
        <div class="card-body">
          <form action="/driver/vehicle" method="POST">
            <div style="display:flex;flex-direction:column;gap:14px;">
              <div class="form-grid">
                <div class="form-group">
                  <label>Make</label>
                  <input type="text" name="make" placeholder="Toyota" required>
                </div>
                <div class="form-group">
                  <label>Model</label>
                  <input type="text" name="model" placeholder="Corolla" required>
                </div>
                <div class="form-group">
                  <label>Color</label>
                  <input type="text" name="color" placeholder="White" required>
                </div>
                <div class="form-group">
                  <label>Plate Number</label>
                  <input type="text" name="plateNumber" placeholder="ABC-123" required>
                </div>
                <div class="form-group full">
                  <label>Seating Capacity</label>
                  <input type="number" name="seatingCapacity" min="1" max="20" placeholder="4" required>
                </div>
              </div>
              <button type="submit" class="btn btn-primary">Register Vehicle</button>
            </div>
          </form>
        </div>
      </div>

      <!-- My Vehicles -->
      <div class="card">
        <div class="card-header">
          <h2>My Vehicles</h2>
          <a href="/driver/register-vehicle" class="btn btn-secondary btn-sm">Register Vehicle</a>
        </div>
        <% if (vehicles.length === 0) { %>
          <div class="empty-state">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="1" y="3" width="15" height="13" rx="2"/><path d="M16 8h4l3 5v3h-7V8z"/><circle cx="5.5" cy="18.5" r="2.5"/><circle cx="18.5" cy="18.5" r="2.5"/></svg>
            <p>No vehicles registered yet.</p>
          </div>
        <% } else { %>
          <div class="table-wrap">
            <table>
              <thead>
                <tr>
                  <th>Make / Model</th>
                  <th>Color</th>
                  <th>Plate</th>
                  <th>Seats</th>
                  <th>Reg. Status</th>
                </tr>
              </thead>
              <tbody>
                <% vehicles.forEach(v => { %>
                  <% const reg = vehicleRegs.find(r => r.VehicleID === v.VehicleID); %>
                  <tr>
                    <td><strong><%= v.Make %></strong> <%= v.Model %></td>
                    <td><%= v.Color %></td>
                    <td><code style="background:var(--gray-100);padding:2px 6px;border-radius:4px;font-size:0.8rem"><%= v.PlateNumber %></code></td>
                    <td><%= v.SeatingCapacity %></td>
                    <td>
                      <% if (!reg) { %>
                        <a href="/driver/register-vehicle" style="font-size:0.78rem;color:var(--red);font-weight:600;text-decoration:none">Not registered</a>
                      <% } else if (reg.Status === 'expired') { %>
                        <span class="badge badge-cancelled">Expired</span>
                      <% } else if (reg.Status === 'suspended') { %>
                        <span class="badge badge-pending">Suspended</span>
                      <% } else { %>
                        <span class="badge badge-active">Active</span>
                      <% } %>
                    </td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>
        <% } %>
      </div>

      <!-- My Rides -->
      <div class="card full-width">
        <div class="card-header">
          <h2>My Recent Rides</h2>
          <span style="font-size:0.8rem;color:var(--gray-400)">Last 10 rides</span>
        </div>
        <% if (rides.length === 0) { %>
          <div class="empty-state">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M3 12h18M3 6h18M3 18h18"/></svg>
            <p>No rides posted yet. Post your first ride above.</p>
          </div>
        <% } else { %>
          <div class="table-wrap">
            <table>
              <thead>
                <tr>
                  <th>From</th>
                  <th>To</th>
                  <th>Departure</th>
                  <th>Seats</th>
                  <th>Status</th>
                </tr>
              </thead>
              <tbody>
                <% rides.forEach(r => { %>
                  <tr>
                    <td><%= r.Origin %></td>
                    <td><%= r.Destination %></td>
                    <td><%= new Date(r.DepartureTime).toLocaleString('en-PK', {dateStyle:'medium', timeStyle:'short'}) %></td>
                    <td>
                      <span style="color:var(--blue-600);font-weight:600"><%= r.AvailableSeats %></span>
                      <span style="color:var(--gray-400)">/ <%= r.TotalSeats %></span>
                    </td>
                    <td><span class="badge badge-<%= r.Status %>"><%= r.Status %></span></td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>
        <% } %>
      </div>

    </div>
  </div>

</body>
</html>
